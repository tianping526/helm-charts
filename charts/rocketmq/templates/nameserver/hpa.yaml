{{- if .Values.nameserver.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rocketmq.nameserver.fullname" . }}
  labels:
    {{- include "rocketmq.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: {{ include "rocketmq.nameserver.fullname" . }}
  minReplicas: {{ .Values.nameserver.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.nameserver.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.nameserver.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.nameserver.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.nameserver.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.nameserver.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
