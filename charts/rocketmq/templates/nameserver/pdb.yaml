{{- $clusterRole := "nameserver" -}}
{{- if .Values.nameserver.podDisruptionBudget.enabled -}}
apiVersion: {{ include "rocketmq.pdb.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ include "rocketmq.fullname" . }}-{{ $clusterRole }}-pdb
spec:
  {{- if .Values.nameserver.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.nameserver.podDisruptionBudget.minAvailable }}
  {{- end }}
  {{- if .Values.nameserver.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.nameserver.podDisruptionBudget.maxUnavailable }}
  {{- end }}
  {{- if (semverCompare ">= 1.27-0" .Capabilities.KubeVersion.Version) }}
  {{- with .Values.nameserver.podDisruptionBudget.unhealthyPodEvictionPolicy }}
  unhealthyPodEvictionPolicy: {{ . }}
  {{- end }}
  {{- end }}

  selector:
    matchLabels:
      {{- include "rocketmq.selectorLabels" $ | nindent 6 }}
      component: {{ $clusterRole }}
{{- end }}